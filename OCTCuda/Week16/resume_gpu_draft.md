# 简历 GPU 草稿（私有；数字占位符以 perf.md 为准）

> 公司经历一律脱敏；开源仓链接放功能模块名。

## 工作经历（脱敏表述示例）
- 参与血管内 OCT 影像软件 GPU 计算模块维护与算法理解，覆盖 回拉成像、实时预览、自动分析 全链路（[技术栈] CUDA / cuFFT / Qt / OpenCV）。
- 与算法/采集团队协同定位 GPU 显存与线程问题，支撑 500+ 帧长回拉的稳定出图与交互刷新。

## GPU 项目/技能条目（bullet，数字待填）
1. 实现 CUDA OCT 实时重建管线（重采样+窗 → cuFFT/Log → Transpose → DSC → 增强/伪彩），e2e _ fps（DSC v2，机型 _）。
2. 实现批量回拉 GPU 处理骨架（整卷 FFT 卷显存管理、帧外层布局、状态机驱动），单卷处理 _ ms。
3. 将 DSC 由逐像素坐标映射优化为行内插（v2），单帧耗时 _ ms → _ ms（-__%）。
4. 实现教学用 IPA 光衰减系数 μ 估计器（每 A-line 滑窗对数拟合），合成三档 μ 相对误差 <15%；拆 Calculate/Update 两阶段支持阈值实时刷新。
5. 设计显存记账 Context（预算检查、按需回拷、故障恢复）与“单帧/全卷/单帧更新”三粒度渲染缓存。
6. 常驻 GPU 线程 + 状态机（非法 API 顺序抛异常）+ 双流帧间流水实验（_ % 提升）。

## 开源项目段
oct-cuda（[link]，教学复现）：
- 管线、IPA、DSC、线程/状态机全部可复现；tests/ 合成数据验收 + docs/perf.md、precision.md。
- 免责声明：教学用途，非医疗产品，参数自定义。

## 一段式自我介绍（口头）
“我在医学影像软件做 GPU 计算：熟悉 CUDA 实时成像管线、cuFFT、批量回拉处理与 IPA 类分析算法；能讲清线程/流/状态机/显存四件事；有完整开源复现仓库与性能、精度文档支撑。”
